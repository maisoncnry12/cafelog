<table class='table  table-inverse'>
<h3>投稿一覧</h3>
<% @posts.each do |post| %>
 <tr id="post_<%= post.id %>">
<div>
  <!--プロフィール-->
  <td><%= image_tag('no_image.jpg') %><%= post.user.name %>
  <!--フォロー機能-->
  <%# if current_user.following?(@user) %>
  <%#= link_to "フォロー外す", user_relationships_path(@user.id), method: :delete, class: "btn btn-primary" %>
  <%# else %>
  <%#= link_to "フォローする", user_relationships_path(@user.id), method: :post, class: "btn btn-success" %>
  <%# end %>
　</td>
  <!--投稿画像-->
  <td><%= attachment_image_tag post, :post_image %></td>
  <!--いいね-->
  <td class="favorite-btn"><%= render "favorites/favorite-btn", post: post %></td>
  <!--ショップ名-->
  <td>Cafe's Name：<%= post.shop_name %></td>
  <!--<p>Area : <%#= post.area_id %></p>-->

  <!--説明-->
  <td>Text：<%= post.body %></td>

  <!--評価-->

         <td id="star-rate-<%= post.id %>">Recommend : </td>
          <script>
          $('#star-rate-<%= post.id %>').raty({
            size: 36,
            starOff:  '<%= asset_path('star-off.png') %>',
            starOn : '<%= asset_path('star-on.png') %>',
            starHalf: '<%= asset_path('star-half.png') %>',
            half: true,
            readOnly: true,
            score: <%= post.rate %>,
          });
          </script>
        <!--/星評価-->

  <!--コメント-->
  <td>Comments :<%= form_with(model:[post, @comment], local: true) do |f| %>
     <%= f.text_area :comment, rows:'3',placeholder: "コメントをここに" %>
     <%= f.submit "Post" %>
     <% end %></td>
     <%# if @comment.user == current_user %>
     <!--<div>-->
      <%#= link_to "削除", post_comment_path(post.id), method: :delete %>
     <!--</div>-->
     <%# end %>




  </div>
  <!--<p>POST</p>-->
 <td>
  <% if post.user == current_user %>
   <%= link_to "Edit", edit_post_path(post.id) %>
  <% end %>
  <% if post.user == current_user %>
   <%= link_to "Destroy", post_path(post.id), method: :delete %>
  <% end %>
 </td>

</div>
</tr>
<% end %>
</table>